.row
  .page-header
    h1 品牌管理&nbsp;
      small &nbsp;品牌列表
.row
  .col-sm-12
    .col-sm-2
      // Standard button
      a.btn.btn-primary.btn-block(href='/admin/brand/new')  添加
    .col-sm-offset-1.col-sm-8
      form.form-inline.pull-right(role='form', ng-submit='queryByName()')
        .form-group
          label.sr-only(for='searchName') 名称
          input#searchName.form-control(type='text', placeholder='输入全称或某一个字', ng-model='queryBrandName')
        button.btn.btn-primary(type='submit') 查询
hr
.row
  .table-responsive
    table.table.table-striped.table-hover
      thead
        tr
          th.col-sm-1 #
          th.col-sm-5 名称
          th.col-sm-2 缩略图
          th.col-sm-2 #
      tbody
        tr(ng-repeat='brand in brands')
          td {{brand.id}}
          td {{brand.name}}
          td
            img.img-circle.img-responsive(src='{{brand.picUrl}}', width='40', height='40')
          td
            .btn-group.btn-group-sm(role='group')
              button.btn.btn-default(type='button', ng-click='edit(brand.id)')
                span.glyphicon.glyphicon-pencil(aria-hidden='true')
              button.btn.btn-danger(type='button', ng-click='remove(brand.id)')
                span.glyphicon.glyphicon-trash(aria-hidden='true')
  paginat